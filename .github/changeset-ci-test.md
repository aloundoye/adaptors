# Changeset CI pass test

This temporary file verifies that the changeset workflow passes a pull request
which contains both a repository change and a changeset. This test pull request
must not be merged.
