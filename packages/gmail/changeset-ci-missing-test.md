# Per-adaptor changeset CI failure test

This temporary file changes the Gmail adaptor package without adding a
changeset, so the disposable test pull request must fail the per-adaptor
changeset check. Do not merge this file.
