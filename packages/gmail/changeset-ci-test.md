# Per-adaptor changeset CI pass test

This temporary file changes the Gmail adaptor package so that the disposable
test pull request exercises the passing per-adaptor changeset path. Do not merge
this file.
